# Quick start:
#   npm run dev --prefix demo
#
# The demo defaults to its hosted private networks; nothing needs configuring.
# To run against local networks instead (see demo/network/README.md), copy
# this file to .env.

VITE_EVM_RPC_URL=http://127.0.0.1:8545
VITE_SOLANA_RPC_URL=http://127.0.0.1:8899
