{
  "name": "mera-docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm --prefix .. ci && npm --prefix .. run build && npm --prefix ../demo ci && npm --prefix ../demo run build -- --outDir ../docs/public/demo --emptyOutDir",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/starlight": "^0.41.3",
    "@fontsource-variable/jetbrains-mono": "^5.2.8",
    "astro": "^7.0.9",
    "sharp": "^0.35.3",
    "unist-util-visit": "^5.1.0"
  },
  "engines": {
    "node": ">=24"
  }
}
